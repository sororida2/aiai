# AI Agent 프레임워크 — 설계 철학

## 기본 철학: 제어에서 위임으로

AI agent를 쓴다는 것의 철학은 **모든 경우의 수를 미리 코드로 명시하는 대신, 판단을 실행 시점으로 위임한다**는 것이다. 기존 소프트웨어는 개발자가 모든 분기를 예측해 코드로 박아두지만, agent는 tool의 존재와 의미(스키마+설명)만 노출해두고 어떤 상황에 무엇을 쓸지는 매 호출마다 모델이 판단한다.

대가는 결정론의 포기다. 같은 입력에도 다르게 반응할 수 있고, tool을 잘못 고를 수 있다. 그래서 agent를 쓴다는 건 "제어권을 일부 내려놓는 대신, 그 불확실성을 관리하는 인프라(하네스·tracing·confidence 구분)를 갖춘다"는 철학이다. 인프라 없이 제어권만 내려놓으면 위험하고, 인프라만 있고 제어권을 안 내려놓으면 기존 하드코딩과 다를 게 없다.

## API 이질성·semantic 불명확성이라는 제약

이 위임의 전제는 모델이 판단할 근거(스키마, 설명, semantic)가 일관되고 신뢰 가능해야 한다는 것이다. API 구조가 제각각이고 semantic이 불명확하면, 위임이 "잘 정의된 일을 넘기는 것"이 아니라 "불확실한 일을 그대로 떠넘기는 것"이 된다.

- "API 추가 + 프롬프트 수정만으로 실시간 확장"은 그 API의 semantic이 이미 파악되어 있을 때만 진짜로 실시간이다.
- 인간의 조사 작업량은 줄어들지 않고, 코드 작성이 문서/매핑 작성으로 형태만 바뀐다.
- 하드코딩은 모르는 케이스에서 명시적으로 죽지만, agent는 모르는 semantic에도 그럴듯하게 추측해 응답할 수 있다 — 실패가 조용히 숨을 위험. 그래서 guardrail의 fail-fast가 필수다.
- Agent 아키텍처가 푸는 건 "제어 흐름을 유연하게 만드는 것"이지 "API의 의미를 알아내는 것"이 아니다. 둘은 독립적인 축이다.

## 결정론적 flow vs Agent 판단의 경계

**원칙: 알려진 것은 고정, 모르는 것만 agent에게 위임.** 순서·의존관계가 이미 알려진 결정론적 지식(예: 레거시 5개를 확인하는 정해진 순서)을 agent가 매번 재추론하게 하는 것은 불필요한 리스크를 스스로 만드는 안티패턴이다.

Agent 판단으로 넘기는 기준:
1. 규칙이 진짜로 맥락 의존적이어서 조건문으로 유한 나열이 안 될 때
2. 규칙이 자주 바뀌어 재배포 비용이 판단 리스크보다 클 때
3. 규칙 자체가 아직 확정되지 않아 사람도 케이스마다 다르게 처리할 때

구조적으로는 **고정 파이프라인(순서·의존관계) + 그 안의 판단 노드(해석이 필요한 지점만 agent)**로 나눈다. 청약진행상황처럼 내부 흐름이 결정론적인 도메인은 Tool Registry에 개별 tool이 아니라 고정된 서브 workflow 전체를 하나의 상위 capability로 등록하는 것이 맞는 설계다.

이 경계는 고정이 아니라 시간이 지나며 이동한다: `inferred` 상태의 판단이 조사·운영 경험으로 `confirmed`가 되면, 그 판단 로직을 고정 규칙으로 승격시키는 절차(승격 프로세스)가 필요하다.

## 분기가 있는 Workflow — 선형이 아니라 그래프

"결정론적 flow"는 일직선을 뜻하지 않는다. 결정론적이라는 건 "각 분기 조건이 코드로 명시 가능하다"는 뜻이지 "분기가 없다"는 뜻이 아니다. 정확한 모델은 **조건부 엣지를 가진 방향 그래프(DAG, 혹은 순환을 허용하는 state machine)**다.

분기 유형은 네 가지로 나뉜다.
1. **결정론적 분기** — 코드값/조건으로 완전히 판별 가능 (예: 상태코드 X→다음 단계, Y→재제출 대기). 100% 코드/state machine으로 고정한다.
2. **순환(loop-back)** — 예: 서류 미비 → 재제출 대기 → 재제출 완료 시 같은 단계로 복귀. 이것도 결정론적이므로 state machine의 사이클로 고정하고, 무한 루프 방지를 위한 최대 재시도/타임아웃 정책을 하네스에 둔다.
3. **병렬 분기** — 서로 의존하지 않는 확인 단계는 순차 강제 없이 병렬 실행 후 병합(join)한다. 순서가 아니라 동시성의 문제로 별도 취급한다.
4. **판단이 필요한 분기(judged branch)** — 조건 자체가 애매해 코드로 못 박을 수 없는 경우. Agent가 실제로 개입하는 유일한 지점이다.

4번에서 핵심은, 이것이 자유 라우팅(tool을 아무거나 고르는 것)과 다르다는 점이다. Judged branch는 **가능한 다음 단계가 이미 유한하게 정의되어 있고, 모델은 그 중 어느 경로로 갈지 분류(classification)만 한다.** 선택지가 닫혀 있어야(bounded) guardrail로 "이 값들 중 하나만 반환해야 한다"는 검증이 가능하고, tracing에서 "왜 그 경로를 골랐는지"만 추적하면 되며 감사가 쉬워진다. 선택지가 열려 있으면(open) 이 모든 게 무너진다.

Annotation 패턴에도 분기 표현이 필요하다.
- 결정론적 엣지 선언: `@workflow_step(order=1, next={"정상": "step_b", "서류미비": "resubmit_wait"})`
- Judged 노드는 선택지를 유한 집합으로 강제: `@judged(choices=["자동승인", "수동검토"])` — 이 제약이 없으면 자유 위임과 구분이 사라진다.

## 범용 프레임워크와 커스터마이즈의 관계

프로젝트마다 customize가 발생하는 건 정상이며, 실패의 증거가 아니다. 기준은 **customize가 엔진(코드) 레벨에서 일어나느냐, 설정/선언 레벨에서 일어나느냐**다.

| 고정되는 것 (엔진) | 매번 바뀌는 것 (설정) |
|---|---|
| 오케스트레이터 라우팅 메커니즘 | 등록되는 tool 목록 |
| Tool Registry 등록 규약 | 각 tool의 실제 스키마 |
| 하네스의 guardrail 체인 실행 순서 | 적용할 guardrail 규칙 |
| 파이프라인 정의 방식 자체 | 실제 단계 순서·판단 지점 위치 |
| confidence 승격 프로세스 절차 | 매핑 테이블의 실제 내용 |

검증 질문: **"다음 프로젝트를 시작할 때 엔진 코드를 한 줄이라도 고쳐야 하는가?"** — 아니오면 범용 프레임워크로서 성공, 예면 프로젝트별 재작성에 가깝다.

## 구현 패턴: Annotation/데코레이터 기반 선언

Spring의 annotation처럼, 설정을 코드 옆에 선언적으로 붙이고 프레임워크가 스캔해서 조립하는 방식을 Python 데코레이터로 구현한다. OpenAI Agents SDK의 `@function_tool`이 이미 이 패턴(함수 시그니처/docstring에서 스키마 자동 추출)의 초기 형태다.

- **Tool Registry**: `@tool(...)` 데코레이터 + 모듈 스캔으로 자동 등록
- **파이프라인 단계**: `@workflow_step(order=1, source="레거시A")` 로 순서 선언
- **Agent 판단 지점**: `@judged(confidence_required="confirmed")` 로 확률적 판단이 들어가는 곳을 명시적으로 마킹
- **Semantic 매핑**: 코드가 아닌 별도 설정 파일(yaml/json)로 관리하되 confidence 필드를 스키마로 강제
- **Guardrail**: `@guardrail(input=..., output=...)` 로 tool/step에 부착

주의: annotation을 남용하면 실제 흐름을 코드만 보고 읽을 수 없게 되는 "magic" 함정에 빠진다. 데코레이터는 (1) registry 자동 등록, (2) 정책 선언(순서/confidence/guardrail)에만 한정하고, 실제 비즈니스 로직은 함수 본문에 명시적으로 남긴다.

## Tool-하네스 결합의 성격

하네스(guardrail)는 tool과 결합되지만 강결합은 아니다. 오케스트레이터가 tool의 입력 스키마에만 커플링되고 내부 구현에는 커플링되지 않는 것과 같은 축의, 반대 방향 커플링이다.

- **결합되는 것**: tool이 `@guardrail(output_schema=...)`처럼 자신의 출력 스키마·검증 규칙을 선언 → 하네스 엔진이 그 선언을 읽어서 실행
- **결합되지 않아야 하는 것**: 하네스 엔진 코드 자체에 특정 tool의 이름이나 도메인 규칙이 하드코딩되는 것 (`if tool_name == "legacy_a": ...` 같은 분기가 나타나면 이건 안티패턴)

MCP 2025-06-18의 outputSchema/structuredContent 기능이 이 원리의 프로토콜 레벨 구현 사례다. tool이 output의 "형태"를 스키마로 선언하면 서버가 그 형태를 강제하지만, 그 안에 들어가는 값이 실제로 의미상 올바른지(semantic normalization)는 여전히 tool 내부 로직의 몫이다. 열거 가능한 필드는 enum으로 강하게 제한해 미확인 원시값(예: 레거시 코드 "99")이 스키마를 그냥 통과하지 못하게 막아야 하며, 이건 ① tool 내부 정규화 로직 ② outputSchema의 enum/type 제약 — 이중 방어로 짠다.
