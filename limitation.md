# 이 프레임워크의 근본적 한계 — 질문의 방법론은 사전에 결정된다

`ai_framework_2.md`가 이 프레임워크의 설계 철학(무엇을 왜 이렇게 만들었는가)을 적은 문서라면, 이 문서는 그 철학을 실제로 써보면서(4개 외부 API 서비스 추가, tool 선택/인자 채우기 실험) 드러난 **구조적 한계**를 적는다. 결함 목록이 아니라, 이 아키텍처가 설계 시점에 이미 선택한 트레이드오프의 다른 얼굴이다.

## 핵심 주장

이 프레임워크는 "AI가 무엇을 결정할 수 있는가의 범위를 넓히는 도구"가 아니라, **"사람이 미리 정해둔 범위 안에서 AI가 그 결정을 얼마나 안전하게 대신하게 할 것인가를 다루는 도구"**다.

- tool 카탈로그는 `discover_services()`가 돌기 전에 이미 닫혀 있다.
- `judged`/`human_action`의 선택지(`choices=(...)`)는 데코레이터를 쓰는 순간 닫힌다.
- `guardrail`의 스키마(`input_schema`/`output_schema`)도 작성 시점에 닫힌다.
- `SemanticMapping`(`mapping.json`)도 사람이 최초 1회 조사해서 고정한 자산이다.

자연어 요청이 들어오는 지점(`Orchestrator.handle(request, **kwargs)`)은 유연해 보이지만, 그 요청이 실제로 착지할 수 있는 자리는 전부 **위 네 가지 중 하나로 미리 파놓은 자리들뿐**이다. 이 프레임워크가 실시간으로 잘 하는 일은 "이미 있는 선택지 중 맞는 걸 고르는 것"(분류)이고, "선택지 자체가 없는 곳에 새 선택지를 만들어내는 것"(진짜 새로운 해석)이 아니다.

## 증거 1 — tool 선택 vs 인자 채우기

`AgentRunner.choose_tool(request, tool_catalog, prompt) -> str`을 실제로 흉내내 자연어 요청 9개(tool 이름을 전혀 언급하지 않은 문장들)를 등록된 8개 tool 카탈로그만 보고 판단해봤다 — 전부 정확히 골랐고, 심지어 "상태 확인하고 날씨도 같이"처럼 두 tool이 필요한 요청은 `subscription_weather_flow`라는 이미 고정된 조합 capability를 정확히 찾아냈다.

반면 같은 방식으로 **인자 채우기**를 시도하자 바로 무너졌다.

| 요청 | 필요한 파라미터 | 실패 유형 |
|---|---|---|
| "이번 크리스마스가 미국에서 공휴일이야?" | `public_holiday(year: int, country_code: str)` | `year`가 요청 어디에도 없음 — "이번"이 뭘 뜻하는지 스키마 어디에도 규칙이 없다 |
| "환율 좀 알려줘, 원화로" | `exchange_rate(base: str, symbols: str)` | "원화로"가 기준(base)인지 조회 대상(symbols)인지 문장만으론 진짜 모호함 |
| "베트남 대학도 찾아줘" | `university_search(country_code: str)` | `VN`으로 맞게 나올 가능성이 높지만, 그건 모델의 일반 지식이 우연히 맞은 것이지 코드가 보장한 게 아니다 |

두 실험의 차이: **tool 선택은 "이미 존재하는 유한한 카탈로그 중 분류"라 이 프레임워크의 강점(bounded classification)과 정확히 일치**했고, **인자 채우기는 "존재하지 않는 값을 그 자리에서 새로 만들어내야" 해서 이 프레임워크가 원래 지양하던 자유 생성 문제**로 바로 넘어갔다.

## 증거 2 — "변환 가능"은 다리가 이미 있을 때만 성립한다

`university_search`(Hipolabs Universities API)와 `public_holiday`(Nager.Date API)는 "국가"를 표현하는 방식이 원래 서로 다르다 — 하나는 ISO 3166-1 alpha-2 코드(`US`), 하나는 자기 데이터셋의 영문 국가명 문자열(`United States`). 이 둘을 잇는 `mapping.json`(200개국)을 실제로 만들 수 있었던 유일한 이유는, **Hipolabs API 자신의 응답 레코드 안에 `alpha_two_code`와 `country`가 이미 같이 들어있었기 때문**이다 — 그 다리를 새로 놓은 게 아니라, 이미 있던 다리를 데이터에서 뽑아낸 것뿐이다(199개국은 깔끔한 1:1, `VN`만 데이터셋 자체의 표기 불일치로 예외).

만약 여섯 번째 API가 "미국"을 또 다른 방식으로 표현하는데, 그 표현과 alpha-2 코드를 이어줄 공통 필드가 그 API 어디에도 없다면 — 이 프레임워크는 변환 테이블을 만들 방법이 없다. 남는 선택지는 둘뿐이다: (1) 외부에서 사람이 참고표를 가져와 검증해서 고정하거나(그래도 결국 "사람이 미리 정해서 닫아두는" 방식), (2) 매번 모델의 추측에 맡기고 감사 가능성을 포기하거나.

**"변환이 가능하다"는 건 시스템이 다리를 놓을 수 있다는 뜻이 아니라, 어딘가에 이미 있는 다리를 찾아낼 수 있었다는 뜻이다.**

## 이게 결함이 아니라 설계상의 선택인 이유

`ai_framework_2.md`의 첫 문단이 이미 이 트레이드오프를 명시하고 있다 — "모든 경우의 수를 미리 코드로 명시하는 대신, 판단을 실행 시점으로 위임한다"는 게 이 프레임워크의 존재 이유인데, 그 위임조차 "어디까지, 어떤 유한한 모양으로" 위임할지는 코드 작성 시점에 사람이 정한다(judged branch는 반드시 bounded choices여야 한다는 원칙). 즉 이 프레임워크는 처음부터 "AI에게 자유를 얼마나 줄 것인가"가 아니라 **"미리 정해둔 좁은 자유를 AI에게 안전하게 위임하는 법"**을 풀도록 설계됐다. 레거시 시스템 연동처럼 감사 가능성(누가/왜 이 값을 골랐는가)이 핵심 요구사항인 도메인에서는 합리적인 선택이다.

## 실무적 함의

- 이 시스템의 "물어볼 수 있는 것의 범위"는 스스로 넓어지지 않는다 — 사람이 새 tool·새 `choices`·새 `mapping.json`을 미리 만들어줘야만 넓어진다.
- 새 서비스를 추가할 때마다(§ `guides/legacy_adapter_guide.md`) "이 tool이 다른 tool과 겹치는 개념을 어떻게 표현하는가"를 사람이 먼저 확인해야 한다 — 코드가 자동으로 감지해주지 않는다(`registry.validate()`/`WorkflowRegistry.validate()`는 참조 무결성만 보지, 의미 중복은 못 본다).
- "인자를 자연어에서 뽑아내는 agent"를 실제로 만들게 되면(현재 없음, `ARCHITECTURE.md`의 한계 항목), 이 문서의 실패 유형 세 가지(암묵적 기본값 없음 / 의미적 모호함 / 검증 안 된 정확성)를 각각 다른 방식으로 다뤄야 한다 — 하나의 해법으로 셋 다 못 잡는다.
- "이 두 시스템의 표현을 자동으로 이어줄 수 있는가"를 판단할 땐, 그 변환을 검증할 **다리(두 표현이 동시에 나타나는 공통 데이터)**가 실제로 존재하는지부터 확인해야 한다 — 없으면 자동화가 아니라 사람이 만드는 고정 자산이 답이다.

---
관련 문서:
- `README.md`의 "근본적 한계 — 질문의 방법론은 사전에 결정된다" 절 — 왜 이 구조가 필요한가 관점에서 같은 한계를 짧게 요약
- `ai_framework_2.md`의 "근본적 한계 — 위임 가능한 범위 자체는 위임되지 않는다" 절 — 이 트레이드오프를 처음 선택한 설계 철학
- `ARCHITECTURE.md`의 "근본적 한계 — 질문의 방법론은 사전에 결정된다" 절 — as-built 구현 근거("열린 질문 요약"의 여러 항목이 이 한계의 다른 표현이라는 연결) + "현재 스캐폴드의 한계"(구체적 미구현 항목들)
- `guides/legacy_adapter_guide.md` — 새 서비스를 추가할 때 이 한계를 실무적으로 어떻게 다루는지
