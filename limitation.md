# 이 프레임워크의 근본적 한계 — 질문의 방법론은 사전에 결정된다

`ai_framework_2.md`가 이 프레임워크의 설계 철학(무엇을 왜 이렇게 만들었는가)을 적은 문서라면, 이 문서는 그 철학을 실제로 써보면서(4개 외부 API 서비스 추가, tool 선택/인자 채우기 실험) 드러난 **구조적 한계**를 적는다. 결함 목록이 아니라, 이 아키텍처가 설계 시점에 이미 선택한 트레이드오프의 다른 얼굴이다.

## 핵심 주장

이 프레임워크는 "AI가 무엇을 결정할 수 있는가의 범위를 넓히는 도구"가 아니라, **"사람이 미리 정해둔 범위 안에서 AI가 그 결정을 얼마나 안전하게 대신하게 할 것인가를 다루는 도구"**다.

- tool 카탈로그는 `discover_services()`가 돌기 전에 이미 닫혀 있다.
- `judged`/`human_action`의 선택지(`choices=(...)`)는 데코레이터를 쓰는 순간 닫힌다.
- `guardrail`의 스키마(`input_schema`/`output_schema`)도 작성 시점에 닫힌다.
- `SemanticMapping`(`mapping.json`)도 사람이 최초 1회 조사해서 고정한 자산이다.

자연어 요청이 들어오는 지점(`Orchestrator.handle(request, **kwargs)`)은 유연해 보이지만, 그 요청이 실제로 착지할 수 있는 자리는 전부 **위 네 가지 중 하나로 미리 파놓은 자리들뿐**이다. 이 프레임워크가 실시간으로 잘 하는 일은 "이미 있는 선택지 중 맞는 걸 고르는 것"(분류)이고, "선택지 자체가 없는 곳에 새 선택지를 만들어내는 것"(진짜 새로운 해석)이 아니다.

## 증거 1 — tool 선택 vs 인자 채우기

`AgentRunner.choose_tool(request, tool_catalog, prompt) -> str`을 실제로 흉내내 자연어 요청 9개(tool 이름을 전혀 언급하지 않은 문장들)를 등록된 8개 tool 카탈로그만 보고 판단해봤다 — 전부 정확히 골랐고, 심지어 "상태 확인하고 날씨도 같이"처럼 두 tool이 필요한 요청은 `subscription_weather_flow`라는 이미 고정된 조합 capability를 정확히 찾아냈다.

반면 같은 방식으로 **인자 채우기**를 시도하자 바로 무너졌다.

| 요청 | 필요한 파라미터 | 실패 유형 |
|---|---|---|
| "이번 크리스마스가 미국에서 공휴일이야?" | `public_holiday(year: int, country_code: str)` | `year`가 요청 어디에도 없음 — "이번"이 뭘 뜻하는지 스키마 어디에도 규칙이 없다 |
| "환율 좀 알려줘, 원화로" | `exchange_rate(base: str, symbols: str)` | "원화로"가 기준(base)인지 조회 대상(symbols)인지 문장만으론 진짜 모호함 |
| "베트남 대학도 찾아줘" | `university_search(country_code: str)` | `VN`으로 맞게 나올 가능성이 높지만, 그건 모델의 일반 지식이 우연히 맞은 것이지 코드가 보장한 게 아니다 |

두 실험의 차이: **tool 선택은 "이미 존재하는 유한한 카탈로그 중 분류"라 이 프레임워크의 강점(bounded classification)과 정확히 일치**했고, **인자 채우기는 "존재하지 않는 값을 그 자리에서 새로 만들어내야" 해서 이 프레임워크가 원래 지양하던 자유 생성 문제**로 바로 넘어갔다.

## 증거 2 — "변환 가능"은 다리가 이미 있을 때만 성립한다

`university_search`(Hipolabs Universities API)와 `public_holiday`(Nager.Date API)는 "국가"를 표현하는 방식이 원래 서로 다르다 — 하나는 ISO 3166-1 alpha-2 코드(`US`), 하나는 자기 데이터셋의 영문 국가명 문자열(`United States`). 이 둘을 잇는 `mapping.json`(200개국)을 실제로 만들 수 있었던 유일한 이유는, **Hipolabs API 자신의 응답 레코드 안에 `alpha_two_code`와 `country`가 이미 같이 들어있었기 때문**이다 — 그 다리를 새로 놓은 게 아니라, 이미 있던 다리를 데이터에서 뽑아낸 것뿐이다(199개국은 깔끔한 1:1, `VN`만 데이터셋 자체의 표기 불일치로 예외).

만약 여섯 번째 API가 "미국"을 또 다른 방식으로 표현하는데, 그 표현과 alpha-2 코드를 이어줄 공통 필드가 그 API 어디에도 없다면 — 이 프레임워크는 변환 테이블을 만들 방법이 없다. 남는 선택지는 둘뿐이다: (1) 외부에서 사람이 참고표를 가져와 검증해서 고정하거나(그래도 결국 "사람이 미리 정해서 닫아두는" 방식), (2) 매번 모델의 추측에 맡기고 감사 가능성을 포기하거나.

**"변환이 가능하다"는 건 시스템이 다리를 놓을 수 있다는 뜻이 아니라, 어딘가에 이미 있는 다리를 찾아낼 수 있었다는 뜻이다.**

## 이게 결함이 아니라 설계상의 선택인 이유

`ai_framework_2.md`의 첫 문단이 이미 이 트레이드오프를 명시하고 있다 — "모든 경우의 수를 미리 코드로 명시하는 대신, 판단을 실행 시점으로 위임한다"는 게 이 프레임워크의 존재 이유인데, 그 위임조차 "어디까지, 어떤 유한한 모양으로" 위임할지는 코드 작성 시점에 사람이 정한다(judged branch는 반드시 bounded choices여야 한다는 원칙). 즉 이 프레임워크는 처음부터 "AI에게 자유를 얼마나 줄 것인가"가 아니라 **"미리 정해둔 좁은 자유를 AI에게 안전하게 위임하는 법"**을 풀도록 설계됐다. 레거시 시스템 연동처럼 감사 가능성(누가/왜 이 값을 골랐는가)이 핵심 요구사항인 도메인에서는 합리적인 선택이다.

## 실무적 함의

- 이 시스템의 "물어볼 수 있는 것의 범위"는 스스로 넓어지지 않는다 — 사람이 새 tool·새 `choices`·새 `mapping.json`을 미리 만들어줘야만 넓어진다.
- 새 서비스를 추가할 때마다(§ `guides/legacy_adapter_guide.md`) "이 tool이 다른 tool과 겹치는 개념을 어떻게 표현하는가"를 사람이 먼저 확인해야 한다 — 코드가 자동으로 감지해주지 않는다(`registry.validate()`/`WorkflowRegistry.validate()`는 참조 무결성만 보지, 의미 중복은 못 본다).
- **(업데이트) "인자를 자연어에서 뽑아내는 agent"를 실제로 만들었다** — `AgentRunner.extract_arguments()`(`framework/orchestrator.py`, `OpenAIRunner`/`FirstMatchRunner` 양쪽 구현, `main.py`). `Orchestrator.handle()`이 `kwargs`에 없는 필드만 추출을 시도하고 호출자가 준 값이 항상 우선하도록 병합해서, `orchestrator.handle("미국에 있는 대학 5개만 보여줘")`처럼 인자 없이 순수 자연어만 던져도 동작한다(스텁 LLM로 실증). 다만 이 문서의 실패 유형 세 가지를 하나의 해법으로 다 잡은 게 아니라, 딱 하나만 다룬다: "확신 없으면 그 필드를 비워두고, 그러면 파이썬이 필수 인자 누락으로 명확히 실패한다"(3번 유형, 검증 안 된 정확성에 대한 최소한의 방어). 1번(암묵적 기본값, `year`의 "이번")과 2번(의미적 모호함, "원화로"가 base/symbols 중 뭔지)은 여전히 모델이 **조용히 하나를 찍어서 틀릴 수 있다** — `confirmed`/`inferred` 신뢰도 태깅이나 모호하면 사람에게 되묻는 일시정지(`AwaitingHumanAction`과 같은 패턴)는 아직 안 붙였다. 즉 "라우팅에 이어 인자까지 채워서 끝까지 도는" 파이프라인은 이제 있지만, 그 결과가 **감사 가능**한 건 아니다 — 이 문서의 핵심 주장("bounded 안에서만 안전하다")은 그대로 유효하고, 지금 이 확장은 그 경계 밖(자유 생성)에 그대로 있다.
- "이 두 시스템의 표현을 자동으로 이어줄 수 있는가"를 판단할 땐, 그 변환을 검증할 **다리(두 표현이 동시에 나타나는 공통 데이터)**가 실제로 존재하는지부터 확인해야 한다 — 없으면 자동화가 아니라 사람이 만드는 고정 자산이 답이다.
- **(업데이트) "고정 조합으로 안 묶인 두 tool을 그때그때 잇는" 시도도 만들었다** — `Orchestrator._resolve_missing_via_other_tool()` + `AgentRunner.rewrite_request()`(`framework/orchestrator.py`, 구현은 `main.py`). "143.248.1.1이 위치한 나라의 대학교 5개를 알려줘"처럼 한 tool의 누락 인자를 다른 tool의 결과로 메워야 할 때, 지원 tool을 찾아 불러서 그 결과를 자연어 요청에 반영해 다시 쓰고 처음부터 재시도한다(§ `ARCHITECTURE.md`의 같은 절 참고). **다만 이건 위 "이 프레임워크식 대응과의 대조"가 말하는 두 선택지(bounded 매핑 승격 / 정준 표현 사전 확정) 중 어느 쪽도 아니다** — `choose_tool()`/`extract_arguments()`/`rewrite_request()` 세 곳 모두 여전히 모델의 자유 생성에 기대는, MCP식 노출 지점을 한 번 더 반복하는 것뿐이다. 다리가 있는지(예: `ip_geolocation`의 출력이 `university_search`의 입력이 이해할 수 있는 형태로 자연어에 녹여지는지) 자체를 검증하진 않고, 안 되면(지원 tool을 못 찾거나 그 호출이 실패하면) 그냥 조용히 포기하고 원래 흐름대로 정직하게(`TypeError`로) 실패한다 — 감사 가능성 문제를 없앤 게 아니라, 실패 지점을 하나 더 늘리면서 "그래도 시도는 해본다"는 최선-노력(best-effort) 계층을 위에 얹은 것이다.

## 업계 비교 — MCP/LangGraph는 이 한계를 어떻게 다루는가

위 논의를 "그럼 업계 표준(MCP)은 이 문제를 어떻게 다루는가? LangGraph처럼 그래프로 직접 연결하면 다른가?"라는 질문으로 한 번 더 파봤다 — 결론은 둘 다 이 한계를 풀지 않고, 같은 지점으로 되돌아온다는 것이었다.

### 문제를 세 계층으로 나누기

같은 실세계 개념(예: "미국")이 실제로는 세 단계를 거쳐야 어떤 tool의 파라미터 값이 된다.

1. **자연어 표면형** — "미국", "the US", "America" (언어·화자에 따라 무한히 다양)
2. **정준(canonical) 표현** — 예: ISO 3166-1 alpha-2 `"US"` (이 프로젝트에서 `public_holiday`/`university_search`가 같은 표현을 쓰도록 의도적으로 맞춘 것 — 새 서비스를 추가하면서 "국가"라는 겹치는 개념을 어떻게 표현할지 사람이 직접 결정해 통일시켰다)
3. **각 API가 실제로 요구하는 형식** — `public_holiday`는 `"US"`, Hipolabs Universities API는 `"United States"`

`university_search/mapping.json`이 다리를 놓은 건 **2→3 구간뿐**이다. 1→2(자연어→정준 표현) 구간은 이 프로젝트도, MCP도, LangGraph도 손대지 않은 영역이다(아래).

### MCP는 이 문제를 풀지 않는다 — 애초에 스코프 밖

MCP(Model Context Protocol)가 표준화하는 건 다음뿐이다:
- tool을 어떻게 목록으로 노출하는가(`tools/list`)
- 어떻게 호출하는가(`tools/call`, JSON-RPC 2.0 포맷)
- 입출력 스키마가 **JSON Schema** 문법을 쓴다는 것(2025-06-18부터 `outputSchema`/`structuredContent`도 추가)

JSON Schema 자체엔 `type`/`enum`/`pattern`/`format` 같은 범용 제약 문법이 있고 `format`에 `"date"`, `"email"`, `"uuid"` 같은 몇 가지 표준값이 있지만, `"iso-3166-1-alpha-2"`처럼 **도메인 특화 정준 표현을 못박는 표준 포맷은 JSON Schema에도 MCP에도 없다.** "국가는 이렇게 표현해라"는 규약은 전적으로 각 tool 작성자가 `description`(자유 텍스트)에 적어두는 것 말고는 강제할 방법이 없다 — tool A가 `"US"`를 원하고 tool B가 `"United States"`를 원해도 프로토콜 위반이 아니다.

**즉 2→3 구간의 파편화는 MCP가 못 막은 게 아니라, 원래 그 계층을 다룰 스코프가 없다.** MCP는 "발견·호출 프로토콜"이고 "의미 규약 표준화"는 처음부터 다른 층의 문제다.

실제로 1→3 전체(자연어에서 각 API가 원하는 정확한 값까지)를 만들어내는 건 MCP 클라이언트/서버가 아니라 **호출 시점의 모델 자신**이다 — 모델이 tool 카탈로그를 보고 어떤 tool을 부를지와 각 파라미터 값을 함께 생성하고, 클라이언트는 그 값을 그대로 서버에 전달한다. 서버의 `inputSchema` 검증은 형태(shape) 검증(타입/enum)이지 "이 값이 사용자 의도를 올바르게 옮긴 것인가"(의미 검증)가 아니다.

안전망이 있다면 `enum` 제약뿐인데, 이것도 **카디널리티에 따라 실효성이 갈린다**:
- **국가 코드(~250개)**: `enum: ["US","KR",...]`로 강제 가능. "존재하는 값"은 보장되지만 "US가 진짜 사용자가 말한 나라인가"는 여전히 검증 안 됨.
- **주식 티커(수만 개)**: enum 자체가 현실적으로 불가능. 대부분의 주식 관련 MCP tool은 `symbol: string`이라고만 선언하고 enum 제약 자체가 없다 — "애플"→`"AAPL"`을 모델이 학습 지식만으로 만들어내고, 존재하는 값인지조차 구조적으로 검증 안 된다. 비슷한 회사명 혼동이나 같은 회사의 클래스 다른 주식(`BRK.A`/`BRK.B`) 오선택이 실제로 이 지점에서 나오는 알려진 실패 유형이다.

### 이 프레임워크식 대응과의 대조

이 레포(`ARCHITECTURE.md`/`ai_framework_2.md`)가 처음부터 고른 트레이드오프는 "judged branch는 반드시 bounded여야 감사 가능하다"였다. MCP의 표준적 실무(위)는 정확히 그 반대 방향 — 감사 가능성을 포기하고 모델의 자유 생성에 맡기는 것이다. 이 프레임워크 방식으로 2→3(또는 1→2) 구간을 안전하게 만들려면 선택지는 결국 두 가지뿐이다:

1. **변환 자체를 bounded tool/매핑으로 승격시킨다** — `university_search`의 어댑터 내부 매핑을 독립 tool(`country_code_lookup(name) -> alpha2`)로 빼서, 에이전트가 실제 API를 부르기 전에 먼저 그걸 부르게 강제한다. 주식 티커처럼 카디널리티가 큰 경우도 원리는 같다 — 거래소가 공개하는 상장 목록 데이터가 그 "다리" 역할을 할 수 있다(Hipolabs 응답 안에 `alpha_two_code`+`country`가 같이 있던 것과 같은 패턴, 규모만 다름).
2. **정준 표현을 명시적으로 정하고 그것부터 시작한다** — 이미 이렇게 했다. `public_holiday`/`university_search`를 추가할 때 "국가"라는 겹치는 개념을 alpha-2로 통일시키기로 사람이 직접 결정했다(우연이 아니라 지시된 것). 다만 이 결정이 아직 코드/문서 어디에도 **명시적으로 선언**돼 있진 않다 — `guides/legacy_adapter_guide.md` §3("다른 tool과 개념이 겹치는지 먼저 확인한다")가 실무적으로는 이 결정을 따르고 있지만, "이 프로젝트의 국가 정준 표현은 alpha-2다"라고 못박아 둔 자리는 없다. 이렇게 명문화해두면 새 서비스가 추가될 때마다 매번 다시 판단하지 않고 "alpha-2 → 그 서비스의 자기 형식"만 책임지면 된다.

두 방법 모두 **1→2(자연어→정준 표현) 구간은 여전히 못 푼다** — 이건 이 프레임워크의 한계가 아니라 업계 전체가 아직 못 푼(혹은 "모델의 일반 지식에 맡긴다"로 타협한) 지점이다.

### LangGraph류 그래프 연결과의 대조 — "풀었다"가 아니라 "문제가 옮겨간다"

MCP식 tool 호출과 LangGraph식 그래프 연결의 결정적 차이는, **엣지(A의 출력 → B의 입력)를 누가, 언제 만드는가**다.

- **MCP**: 매 호출마다 **모델이 호출 시점에** 인자를 새로 생성한다 — 위의 문제가 그대로 발생.
- **LangGraph**: 그래프의 엣지가 보통 **개발자가 그래프 설계 시점에** 코드로 직접 짠다(공유 `state`의 한 필드를 다음 노드 호출의 인자로 그대로 넘기는 식). 이건 이 레포의 `subscription_weather_flow`가 `context["subscription_result"]["region"]`을 `weather(location=region)`에 넘기는 것과 정확히 같은 패턴이다 — 그 순간 LLM은 전혀 관여하지 않는다.

그래서 LangGraph는 이 disparity 문제를 **풀어낸 게 아니라, 발생 지점을 "호출 시점의 모델"에서 "그래프 설계 시점의 개발자"로 옮긴 것**이다. 두 끝을 같은 사람/팀이 설계하니 (1) 애초에 같은 `state` 스키마를 쓰면 두 노드가 같은 필드를 다르게 해석할 여지가 잘 안 생기고, (2) 표현이 안 맞으면 그 엣지에 변환 코드를 한 번 박아두면 결정론적으로 고정된다 — MCP처럼 매번 모델이 다시 추측하지 않는다.

**다만 문제가 완전히 없어지진 않는다 — 그리고 "체이닝 여부"는 정확한 기준이 아니다.** 처음엔 "그래프 엣지로 연결되는가"를 기준으로 삼았는데, 더 정확한 기준은 **그 엣지가 외부의, 남이 설계한 시스템 경계를 넘는가**다.

LangGraph의 대표 예제로 자주 보이는 건 다단계 API 체이닝이 아니라 "생성 → 스스로 비평 → 재생성" 같은 **응답 정제 루프**(Self-RAG, reflection/critique-revise류)다. 이런 루프의 노드들(`generate`/`critique`/`revise`)은 전부 그래프 자신이 소유한 내부 `state`의 필드(보통 텍스트 문자열 하나)만 읽고 쓴다 — 전부 같은 사람이 같은 순간에 설계한 **닫힌 우주** 안에서만 움직이니, 애초에 "형식이 서로 다른 독립된 두 시스템"이 등장할 자리가 없다. 그러니 이런 예제들이 disparity 문제를 안 보여주는 건 LangGraph가 그 문제를 풀어서가 아니라, **외부 경계를 아예 안 넘기 때문**이다.

이 경계가 실제로 재등장하는 지점은 둘이다:
1. LangGraph 노드 자체가 ReAct류 agent라서 내부적으로 MCP/function-calling을 쓰는 경우 — 그 노드 안에서는 위의 문제가 그대로 재현된다.
2. 그래프가 자기 팀이 안 만든 외부 API를 불러야 하는 경우(`university_search`/`public_holiday`처럼) — 결국 누군가 변환 코드를 짜야 하는 건 동일하고, LangGraph가 이걸 자동으로 안 해주는 것도 MCP와 같다. (참고로 RAG류가 끼워 넣는 검색/리트리버 tool은 외부 경계를 넘긴 하지만 "질의 문자열 → 문서 청크 목록"이라는 형태가 벤더 사이에 꽤 수렴돼 있어서, 국가/통화/티커처럼 벤더마다 제각각인 개념보다는 disparity가 덜 드러난다.)

**부수적으로 확인된 것**: LangGraph가 순수 다단계 API 체이닝(외부 경계를 여러 번 넘지만 순환은 없는 것)보다 응답 정제 루프 쪽 예제가 많은 것도 이 구조와 맞물린다 — 순수 순차 체이닝은(이 레포의 `subscription_weather_flow`처럼) 그래프의 핵심 무기인 "순환(cycle)"이 굳이 필요 없어서 평범한 함수 호출 나열로도 충분하고, "생성↔평가" 순환은 진짜로 그래프 구조가 필요한 지점이라 그래프 프레임워크의 대표 예제로 자주 등장하는 것으로 보인다.

**이 레포 자신이 이미 그 증거다.** `WorkflowRegistry`+`StateMachine`은 소규모 LangGraph다. 이 프로젝트가 한동안 disparity 문제를 실제로 겪지 않았던 이유도 같다 — `main.py`의 데모 호출들이 한동안 전부 사람이 미리 정확한 값(`country_code="US"`)을 채워서 부르고 있어서, "자연어에서 인자를 추출하는" 단계 자체가 없었다. **이후 그 인자 추출 agent(`AgentRunner.extract_arguments()`)를 실제로 만들었고**(위 "실무적 함의" 절의 업데이트 참고), 예측한 대로 이 프로젝트도 MCP식 노출 지점을 그대로 마주치게 됐다 — `orchestrator.handle("미국에 있는 대학 5개만 보여줘")`가 실제로 동작하는 건 정확히 "모델이 호출 시점에 인자를 자유 생성"하기 때문이고, LangGraph 스타일(그래프 엣지의 데이터 흐름)을 쓰고 있다는 사실 자체가 이 문제를 막아주지는 않는다는 예측이 실증으로 확인된 것이다.

**한 단계 더 나간 지점**: `subscription_weather_flow`(엣지가 사람이 코드로 고정한 LangGraph식)와 `orchestrator.handle("143.248.1.1이 위치한 나라의 대학교 5개를 알려줘")`(엣지 자체를 실행 시점에 모델이 찾아 만드는 `_resolve_missing_via_other_tool()`, 위 "실무적 함의" 절 업데이트 참고)를 나란히 놓으면, 이 표의 두 축이 섞이지 않는다는 걸 보여준다 — 후자는 "엣지를 누가, 언제 만드는가"의 답이 "모델이, 호출 시점에"라서 여전히 MCP 쪽에 속하고, LangGraph처럼 사람이 미리 고정한 게 아니다. 즉 엣지를 **동적으로** 만들 수는 있지만, 그 대가로 매 홉(sub-tool 선택 → sub-tool 인자 채움 → 결과를 자연어로 재작성 → 재파싱)마다 disparity 위험이 반복된다 — LangGraph의 "사람이 한 번 고정하면 그 뒤로는 결정론적"이라는 이점은 이 경로에서 얻을 수 없다.

### 비교 결론

MCP는 "입력을 어떤 코드 방식으로 받을지"를 규정하는 I/O 스펙을 갖고 있지 않고, 그 자리를 메우는 건 호출 시점의 모델이다. LangGraph는 그래프 엣지가 개발자 손으로 고정되는 구간에서는 disparity가 구조적으로 덜 생기지만, agent 노드 내부나 외부 API 경계에서는 MCP와 똑같은 문제가 재현된다 — **"풀었다"가 아니라 "발생 지점을 옮겼다"**는 게 두 경우 모두에 적용된다. 결국 감사 가능성을 지키려는 시스템(이 프레임워크 포함)이 할 수 있는 건 자유 생성을 bounded tool/매핑으로 대체하는 것뿐이고, 그 대체가 가능한 유일한 조건은 위에서 이미 확인한 것과 같다 — **두 표현을 잇는 다리가 실제 데이터 어딘가에 이미 있어야 한다.**

---
관련 문서:
- `README.md`의 "근본적 한계 — 질문의 방법론은 사전에 결정된다" 절 — 왜 이 구조가 필요한가 관점에서 같은 한계를 짧게 요약
- `ai_framework_2.md`의 "근본적 한계 — 위임 가능한 범위 자체는 위임되지 않는다" 절 — 이 트레이드오프를 처음 선택한 설계 철학
- `ARCHITECTURE.md`의 "근본적 한계 — 질문의 방법론은 사전에 결정된다" 절 — as-built 구현 근거("열린 질문 요약"의 여러 항목이 이 한계의 다른 표현이라는 연결) + "현재 스캐폴드의 한계"(구체적 미구현 항목들)
- `guides/legacy_adapter_guide.md` — 새 서비스를 추가할 때 이 한계를 실무적으로 어떻게 다루는지
